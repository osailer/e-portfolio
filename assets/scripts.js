// Simple script to highlight the nav active page (optional, since already handled with class).
// Place for extra interactivity if you want (e.g., theme toggles).

// Example: Responsive nav toggle or theme switch placeholder
document.addEventListener("DOMContentLoaded", function() {
  // Future interactive features can go here.
});